.build/
.App/
*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/
.DS_Store
dist/

# Community API — kept locally / deployed from private workspace; not in public repo
community-api/

# Maintainer scripts (release/package/dev) — local only
scripts/

# Local agent design/plan notes (not shared via repo)
docs/superpowers/**
!docs/superpowers/specs/
!docs/superpowers/specs/**
.superpowers/

# Minisign secret key — never commit
burnrate.key
