.build/
.App/
*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/
.DS_Store
dist/

# Local agent design/plan notes (not shared via repo)
docs/superpowers/

# Local maintainer release helper (not shared via repo)
scripts/release.sh

# Minisign secret key — never commit
burnrate.key
